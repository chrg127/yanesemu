=== Directory description ===

    video.*     The video interface, where graphics should be displayed. It is
                built as a generic interface, with support for different
                libraries.
    videosdl.*  The interface using SDL2.
